<%= form_with(model: train_block) do |form| %>
  <% if train_block.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(train_block.errors.count, "error") %> prohibited this train_block from being saved:</h2>

      <ul>
        <% train_block.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :train_plan_id, style: "display: block" %>
    <%= form.text_field :train_plan_id %>
  </div>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_area :title %>
  </div>

  <div>
    <%= form.label :sequence_number, style: "display: block" %>
    <%= form.number_field :sequence_number %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
